# Clippy configuration for narrow

# Prevent large futures from blowing the stack
future-size-threshold = 10000

# Allow wildcard imports from these modules
allowed-wildcard-imports = [
    "constants",
    "errors",
    "super",
    "types",
]
